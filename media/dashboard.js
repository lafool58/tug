// Data containing full translation texts for modal rendering
const translatorData = {
    leedongjin: {
        outlet: "📰 Btv 이동진의 파이아키아 / 왓챠 평론",
        title: `"존 파브로가 스타워즈 세계관에 다시 불어넣은 숨결"`,
        author: "이동진 평론가",
        score: "★★★★☆ (4.0)",
        gradient: "linear-gradient(135deg, #1e272e 0%, #2f3640 100%)",
        link: "https://pedia.watcha.com/",
        sections: [
            {
                title: "🚀 스타워즈 시네마틱 부활의 교두보",
                text: "《만달로리안 앤 그로구》(2026)는 스트리밍 화면에 묶여 있던 프랜차이즈의 활력을 스크린의 광활한 스케일로 성공적으로 전이시킨 야심 찬 시도다. 존 파브로 감독은 기존 디즈니 플러스 텔레비전 시리즈에서 다져온 두 캐릭터의 유대와 친밀감을 극장판 특유의 시네마스코프 비율(2.39:1) 속으로 완벽하게 용해시킨다. 딘 자린의 고뇌와 그로구의 성장이 가벼운 에피소드식 구성에서 벗어나, 은하 전체를 위협하는 거대한 서사의 주축으로 격상되는 연출적 결단력은 영화관을 찾는 관객들에게 웅장한 카타르시스를 준다. 특히 성간 비행과 다채로운 우주 행성들의 디테일한 비주얼 묘사는 화면을 압도한다."
            },
            {
                title: "🟢 포스의 아날로그적 정취와 감정의 온도",
                text: "영화는 디즈니의 최근 3부작이 노출했던 과도한 CG와 볼거리 중심의 피로감에서 벗어나, 스타워즈 오리지널 3부작이 사랑받았던 거칠고 투박한 아날로그 프로덕션 질감과 미니어처적 정서적 매력을 현대적 감각으로 재탄생시킨 데 가장 큰 성취가 있다. 딘 자린의 차가운 베스카 갑옷과 그로구의 보드라운 감각이 맞닿는 손길, 깊은 침묵 속에서 오가는 시선과 온화한 제스처 하나만으로도 신파적인 대사 없이 깊은 울림을 전한다. 포스를 사용할 때 그로구가 뿜어내는 정서적 긴장감은 관객의 감정적 온도를 서서히 끌어올리며, 클라이맥스에서의 포스 배리어 생성 씬은 클래식 스타워즈 팬들이 갈구했던 온전히 회복된 우주적 판타지의 경이로운 쾌감을 안겨준다."
            },
            {
                title: "🪐 서사적 완결성과 장르적 깊이",
                text: "이번 극장판은 단순히 드라마의 스핀오프나 연장선에 그치지 않고, 두 주인공이 지닌 운명 공동체로서의 서사적 깊이를 완전히 새로운 차원으로 끌어올린다. 은하계의 패권을 둘러싼 정치적 암투나 웅장한 우주 전쟁의 소용돌이 속에서도, 영화는 '아버지가 된 전사'와 '그의 소중한 아이'라는 정서적 초점을 단 한 순간도 잃지 않는다. 딘 자린의 헬멧 속 보이지 않는 눈빛과 흔들림 없는 태도, 그리고 그로구의 천진난만함이 마주하며 빚어내는 장엄한 관계는 은하계를 가로지르는 비행 속에서 깊은 비장함과 가슴 벅찬 감동을 자아내며 시네마틱 스펙터클을 넘어선 서사극의 걸작으로 자리매김한다."
            },
            {
                quote: "은하계 너머로 뻗어나가는 장엄한 모험과 그 속에서 칼날처럼 차갑고도 따뜻하게 타오르는 딘과 그로구의 온화한 드라마. 스펙터클 속에서도 인간성의 깊이를 포기하지 않는 존 파브로의 뚝심.",
                quoteSource: "이동진 평론가 (평점 4.0 / 5.0)"
            }
        ]
    },
    almostmainstream: {
        outlet: "🎬 유튜브 영화 비평 채널 · 거의없다",
        title: `"드라마를 보지 않았어도 두 사람의 케미에 단숨에 몰입된다"`,
        author: "유튜버 거의없다",
        score: "8.5/10",
        gradient: "linear-gradient(135deg, #d35400 0%, #e67e22 100%)",
        link: "https://www.youtube.com/@almost_mainstream",
        sections: [
            {
                title: "🍼 극장용 스케일로 폭발하는 그로구의 포스 웅장함",
                text: "유튜브 크리에이터 거의없다는 이번 영상 비평을 통해, 그로구의 포스 능력이 TV 스크린을 넘어 IMAX 및 특별관의 초거대 스케일에서 어떻게 관객들의 전율을 이끌어내는지에 대해 정밀하게 메스를 들이댔습니다. 드라마 시즌에서 보여줬던 그로구의 제한된 초능력 묘사는 이번 영화에서 시네마틱 장르의 정점으로 확장되었습니다. 제국 잔당의 맹렬한 폭격과 무자비한 추격 속에서 그로구가 포스 배리어를 생성하며 딘 자린을 안전하게 보호하는 시각적 쾌감은 단순히 컴퓨터 그래픽의 승리가 아니라, 감정을 묵직하게 뒤흔드는 역사적인 명장면이라고 극찬했습니다. 극장 사운드를 꽉 채우는 웅장한 오케스트레이션과 포스의 울림은 관객들을 전율하게 만듭니다."
            },
            {
                title: "🛡️ 묵직한 만도의 건파이트 액션과 영리한 편집",
                text: "또한 영화 속 페드로 파스칼의 묵직하고 절도 있는 슈트 연기와 호소력 짙은 보이스 더빙은 얼굴을 한 번도 노출하지 않은 채 오직 행동과 호흡만으로 딘 자린의 내면적 고뇌와 단단한 신념을 고스란히 웅변합니다. 거의없다는 제국군과의 거친 1대 다수 백병전, 절벽을 오가는 제트팩 추격전, 그리고 은하계를 뒤흔드는 대형 스페이스 독파이트 씬 등이 1초의 지루함도 없이 유기적으로 조율되고 영리하게 편집된 점에 격한 찬사를 보냈습니다. 시각 효과의 남발 대신, 실물 스턴트와 타격감을 극한으로 끌어올린 연출은 액션 영화로서 완벽에 가깝습니다."
            },
            {
                title: "⚖️ 일반 대중 관객과 팬층을 모두 포섭하는 영리한 온보딩",
                text: "특히 이번 작품은 디즈니 플러스의 기존 시즌 드라마를 보지 않은 대중 관객도 서사와 인물의 감정선에 순식간에 녹아들 수 있도록 배려한 친절하고 영리한 도입부 설계를 갖고 있습니다. 전작의 방대한 줄거리를 군더더기 없는 대사와 오프닝 시퀀스를 통해 자연스럽게 요약해주면서, 곧바로 거대한 우주 활극으로 질주하는 전개는 상업 영화로서 모범적인 태도를 보여줍니다. 흔하디 흔한 할리우드의 억지 신파나 무의미한 유머를 배제하고, 우주 개척 시대의 거칠고 쓸쓸한 고독함과 두 인물의 따뜻한 교감을 이토록 훌륭한 밸런스로 버무려낸 파브로의 연출력은 대단합니다."
            }
        ]
    },
    justinchang: {
        outlet: "📰 Variety · 저스틴 창 (Justin Chang)",
        title: `"스타워즈 사가에 새로운 활력을 불어넣는 거대하면서도 따뜻한 서사시"`,
        author: "저스틴 창 (Variety 수석 평론가)",
        score: "8.5/10",
        gradient: "linear-gradient(135deg, #1abc9c 0%, #16a085 100%)",
        link: "https://variety.com/",
        sections: [
            {
                title: "🌌 시네마틱 스케일과 인간적 유대의 마법 같은 공존",
                text: "Variety의 저스틴 창은 이 영화가 텔레비전 매체가 주는 친밀함(intimacy)의 강점을 극대화하면서도, 영화관 특유의 장엄한 2.39:1 와이드스크린 스케일로 자연스럽게 도약한 솜씨를 높게 샀습니다. 거대한 성간 비행과 다채로운 행성들이 뿜어내는 스펙터클은 관객들의 눈을 황홀하게 만드는 시각적 대향연을 선보입니다. 그러나 더욱 훌륭한 점은 이러한 막대한 스케일의 홍수 속에서도, 서사의 진짜 원동력인 딘과 그로구의 깊고 정서적인 유대와 정체성 탐구가 단 한 순간도 흔들리지 않고 균형을 유지하고 있다는 사실입니다."
            },
            {
                title: "🔥 스타워즈 사가의 심장을 복원한 연출적 뚝심",
                text: "그는 존 파브로 감독이 할리우드가 잊어가던 아날로그적 향수와 풍부한 정서적 기교의 공존을 일구어냈다고 분석했습니다. 최근 스타워즈 영화들이 지나치게 거대한 설정 놀이나 이념적 대립에 매몰되어 지루해진 반면, 본작은 '아이를 지키기 위해 우주를 달리는 고독한 전사'라는 원초적이고 클래식한 웨스턴 장르의 영혼을 완벽히 소생시켰습니다. 딘의 단단한 베스카 헬멧에 투영되는 은하계의 차가운 빛과, 그 안에서 피어나는 그로구의 작은 손짓은 그 자체로 시네마틱 아트의 진수를 보여줍니다."
            },
            {
                title: "🧭 후반부 서사의 다소 복잡하고 숨 가쁜 호흡",
                text: "다만 저스틴 창은 단점으로 후반부의 과도한 서사 전개와 복잡해진 플롯의 결을 언급했습니다. 다양한 제국 잔당 분파와 은하 공화국 내 복잡한 이해관계가 개입하기 시작하는 3막의 추격 씬들은 제한된 러닝타임 속에서 다소 조급하게 몰아쳐, 초반부가 가졌던 낭만적이고 쓸쓸한 여백의 아름다움을 일부분 잠식한다는 점을 냉철하게 짚었습니다. 그럼에도 불구하고, 극의 엔딩에서 선사하는 두 인물의 묵직한 마무리는 이러한 플롯의 혼선을 말끔히 씻어내기에 충분한 깊이가 있습니다."
            },
            {
                quote: "스타워즈 사가에 위대하고 따뜻한 심장을 이식한 대작. 존 파브로는 할리우드가 잊어가던 아날로그의 향수와 정서적 기교의 공존을 일구어냈다.",
                quoteSource: "저스틴 창 (Variety 평론가)"
            }
        ]
    },
    guardian: {
        outlet: "📰 The Guardian · 피터 브래드쇼 (Peter Bradshaw)",
        title: `"스페이스 오페라의 정점을 다시 찍은 딘과 그로구의 서사시"`,
        author: "피터 브래드쇼 (The Guardian 수석 영화 비평가)",
        score: "★★★★☆ (4.0)",
        gradient: "linear-gradient(135deg, #3498db 0%, #2980b9 100%)",
        link: "https://www.theguardian.com/",
        sections: [
            {
                title: "🛸 클래식 삼부작의 영혼과 우주적 모험심의 완전한 복원",
                text: "The Guardian의 수석 영화 비평가 피터 브래드쇼는 이 작품을 '1977년 조지 루카스의 클래식 오리지널 3부작이 선보였던 우주 활극 본연의 낭만적이고 거친 모험심이 가장 이상적으로 숨 쉬는 걸작'이라고 극찬했습니다. 그는 최근 디즈니가 주도한 스타워즈 시퀄 시리즈가 철학적인 척 무게를 잡으며 억지 설정을 들이대다 놓쳐버렸던 '우주 개척 시대의 본질적인 설렘과 경외감'을 이번 극장판이 마침내 은막 위에 고스란히 복원하는 데 성공했다고 평했습니다."
            },
            {
                title: "🎻 압도적인 음악과 시각적 경이로움의 앙상블",
                text: "특히 영화 전체를 관통하는 루트비히 요란손의 묵직하고 서정적인 메인 클래식 테마와 거대한 오케스트레이션 사운드는 아이맥스의 압도적인 영상과 완벽하게 맞물려 관객의 감각을 사로잡습니다. 아날로그 미니어처와 최첨단 스테이지 크래프트 기술을 결합하여 완성해 낸 다양한 은하계 도시와 모래 언덕, 신비한 포스의 사원 등은 단순한 그래픽을 넘어 실제 살아 숨 쉬는 공간 같은 생생한 리얼리티를 확보했습니다. 이는 관객이 딘 자린와 그로구의 비행선 조종석에 함께 앉아있는 듯한 경이로운 몰입감을 줍니다."
            },
            {
                title: "🌌 현대 SF 장르가 지향해야 할 정서적 방향성",
                text: "브래드쇼는 특수효과와 시각적 자극에만 몰두하며 속 빈 강정처럼 변해가는 현대 할리우드 SF 블록버스터 장르에 이 영화가 훌륭한 경종을 울리고 있다고 분석했습니다. 관객들이 진정으로 열광하고 눈물 흘리는 것은 억만달러짜리 폭발 씬이 아니라, 쇠붙이로 가득한 세상 속에서 서로를 믿고 의지하는 두 인물의 정서적 관계성이라는 사실을 이 영화는 매우 훌륭하고 엄숙한 태도로 입증해 보였습니다."
            }
        ]
    },
    stuckmann: {
        outlet: "🎬 유튜브 영화 전문 비평가 · Chris Stuckmann",
        title: `"This is the way! 스타워즈의 진정한 황금기가 돌아왔다"`,
        author: "크리스 스턱만",
        score: "A- (8.5/10)",
        gradient: "linear-gradient(135deg, #8e44ad 0%, #9b59b6 100%)",
        link: "https://www.youtube.com/",
        sections: [
            {
                title: "🌌 오랜 팬들의 상처를 말끔히 치료해 준 기념비적 시네마",
                text: "영화 전문 크리에이터 크리스 스턱만은 흥분을 감추지 못한 채 비평 영상을 시작하며, 《만달로리안 앤 그로구》(2026)가 시퀄 시리즈와 여러 실망스러운 스타워즈 스핀오프들에 지쳐있던 올드 팬들에게 선사하는 극적 카타르시스와 치유의 과정에 대해 감동적으로 역설했습니다. 그는 존 파브로 감독이 단순히 올드 팬들의 노스탤지어를 값싸게 자극하는 수준을 아득히 뛰어넘어, 두 캐릭터가 가진 진실한 유대와 서사적 매력을 아주 섬세하고 깊이 있게 조율해 내며 스타워즈 최고의 극장 체험을 구현했다고 보았습니다."
            },
            {
                title: "🤖 그로구의 살아있는 애니마트로닉스와 숨 막히는 몰입감",
                text: "스턱만은 특히 CG로 빚어낸 영혼 없는 캐릭터들과 달리, 실물 인형과 애니마트로닉스 기술을 고집스럽게 사용하여 구현된 그로구의 생생하고 귀여운 표정 연기에 큰 찬사를 아끼지 않았습니다. 화면 속에서 그로구가 딘 자린의 손가락을 꽉 쥐거나, 헬멧의 반사광을 보며 방긋 웃는 미세한 눈꺼풀의 움직임은 실제 살아있는 유기체와 같은 완벽한 입체감을 선사하며, 관객이 그들의 감정선에 200% 동화되어 영화의 매 장면에 깊이 침잠할 수밖에 없도록 만드는 결정적인 매력으로 작용한다고 강조했습니다."
            },
            {
                title: "🛡️ This is the way! 은하계를 물들이는 진정한 감동",
                text: "그는 대규모 스페이스 독파이트와 황량한 개척 행성에서의 긴장감 넘치는 추격전들이 영화 전체의 템포를 지루할 틈 없이 이끌고 가며, 후반부 거대한 위협 앞에서도 서로의 신조(Creed)를 지키기 위해 행동하는 딘 자린의 비장한 건파이트 액션은 눈시울을 붉히게 만들기에 충분하다고 평했습니다. 스타워즈의 영광스러운 오리지널리티와 새로운 시대의 트렌드가 가장 모범적으로 융합된 최고의 엔터테인먼트이자 팬들의 열광적인 지지를 이끌어낼 기념비적인 역작이라는 최종 평가를 전했습니다."
            }
        ]
    },
    colony_leedongjin: {
        outlet: "📰 Btv 이동진의 파이아키아 / 왓챠 평론",
        title: `"거대한 담론에 짓눌린 인물들"`,
        author: "이동진 평론가",
        score: "★★★☆ (3.5)",
        gradient: "linear-gradient(135deg, #8e44ad 0%, #9b59b6 100%)",
        link: "https://pedia.watcha.com/",
        sections: [
            {
                title: "🎬 군중이라는 괴물, 개별성의 소멸",
                text: "이동진 평론가는 연상호 감독의 <군체>가 그리는 디스토피아의 핵심을 '개별성의 상실'로 보았습니다. 영화 속 좀비들은 더 이상 단순한 괴물이 아닙니다. 그들은 '집단 지성'이라는 이름 하에 완벽하게 통제되고 조직된 하나의 거대한 유기체, 즉 '군체' 그 자체입니다. 이는 현대 사회에서 알고리즘과 집단 사고에 의해 획일화되어 가는 인간 군상에 대한 탁월한 은유로 기능합니다."
            },
            {
                title: "🎭 장르의 외연 확장, 그러나 줄어든 여백",
                text: "그러나 이동진 평론가는 영화가 담아내려는 철학적 담론이 너무 거대해진 나머지, 정작 영화를 이끌어가는 인물들이 숨 쉴 공간이 부족해졌다고 아쉬움을 표합니다. 세계관을 설명하고 묘사하는 데 힘을 쏟으면서 등장인물들은 다소 평면적인 장기말처럼 움직이는 경향이 있습니다."
            },
            {
                quote: "집단 지성 좀비라는 새로운 개념이 만들어내는 아크로바틱한 공포. 그러나 거대한 담론에 짓눌린 인물들이 만들어내는 빈약한 파장.",
                quoteSource: "이동진 평론가 (평점 3.5 / 5.0)"
            }
        ]
    },
    colony_heojiwung: {
        outlet: "📰 씨네21 — 허지웅의 경사기도권 · 국내 장문 평론",
        title: `"K-좀비의 진화와 딜레마"`,
        author: "허지웅 평론가",
        score: "★★★☆ (3.5)",
        gradient: "linear-gradient(135deg, #2c3e50 0%, #34495e 100%)",
        link: "http://www.cine21.com/",
        sections: [
            {
                title: "🎬 집단 지성이라는 새로운 무기",
                text: "연상호 감독의 신작 <군체>는 그가 개척한 '한국형 좀비'의 진화를 보여줍니다. 개별적으로 달려드는 단순한 감염체를 넘어, 마치 하나의 거대한 유기체처럼 서로 정보를 교류하고 진화하는 '집단 지성 좀비'의 등장은 시각적으로 대단한 압도감을 줍니다. 특히 고층 건물과 비좁은 실내 공간을 아크로바틱하게 활용하는 액션 시퀀스는 이 영화의 가장 큰 성취입니다."
            },
            {
                title: "🎭 기술적 성취 뒤에 가려진 감정적 여운",
                text: "그러나 허지웅 평론가는 영화가 거대한 시스템과 군중의 은유에 집중한 나머지, 개별 캐릭터들이 그 스펙터클을 위한 기능적 도구로 소모되는 측면을 지적합니다. 전지현, 구교환 등 걸출한 배우들의 앙상블은 훌륭하지만, 각자가 지닌 서사의 밀도는 이전 작인 <부산행>이 주었던 감정적 파도에 미치지 못합니다."
            },
            {
                quote: "기술적 성취는 눈부시나 감정적 여운은 다소 옅다. 우리는 좀비 떼의 진화에 놀라면서도, 결국 잊을 수 없는 것은 인간의 나약하고도 이기적인 맨얼굴이라는 것을 깨닫게 된다.",
                quoteSource: "허지웅 평론가, 씨네21 '허지웅의 경사기도권'"
            }
        ]
    },
    colony_guardian: {
        outlet: "📰 The Guardian — Full Review Translation (★★★★☆ 4/5)",
        title: `"숨 쉴 틈 없이 질주하는 롤러코스터"`,
        author: "피터 브래드쇼 (The Guardian 수석 영화 비평가)",
        score: "★★★★☆ (4.0)",
        gradient: "linear-gradient(135deg, #1e5128 0%, #145a32 100%)",
        link: "https://www.theguardian.com/",
        sections: [
            {
                title: "🎬 감염체 장르의 재발명",
                text: "The Guardian의 피터 브래드쇼는 <군체>가 기존의 좀비 영화 문법을 영리하게 비틀었다고 평합니다. 현대 사회의 초연결성과 SNS 알고리즘에서 영감을 받은 듯한 '집단 지성 좀비'는 단순한 괴물이 아니라, 시스템화된 재난 그 자체로 묘사됩니다. 이들이 하나의 거대한 파도처럼 움직일 때의 그 시각적 충격은 칸 영화제의 미드나잇 스크리닝을 열광시키기에 충분했습니다."
            },
            {
                title: "🎭 아쉬움 속에서도 빛나는 속도감",
                text: "물론 다소 도식화된 캐릭터 설정과 급하게 마무리되는 듯한 3막의 전개는 아쉽습니다. 하지만 이 영화가 목표로 하는 '숨 막히는 미드나잇 스릴러'로서의 가치는 충분히 증명해냅니다."
            },
            {
                quote: "감염체 장르를 완전히 새롭게 발명해 낸 숨 막히는 야간 열차. 거대한 야심만큼 빈틈도 있지만, 그 속도감 앞에서는 아무것도 생각할 수 없다.",
                quoteSource: "The Guardian, 피터 브래드쇼 (★★★★☆ 4/5)"
            }
        ]
    },
    colony_indiewire: {
        outlet: "📰 IndieWire — Full Review Translation (Grade: B-)",
        title: `"스펙터클을 위해 희생된 감정적 코어"`,
        author: "데이비드 에를리치 (IndieWire 수석 평론가)",
        score: "★★★☆☆ (3.0)",
        gradient: "linear-gradient(135deg, #27ae60 0%, #2ecc71 100%)",
        link: "https://www.indiewire.com/",
        sections: [
            {
                title: "🎬 전대미문의 좀비 안무(Choreography)",
                text: "IndieWire의 데이비드 에를리치는 연상호 감독이 <반도> 이후 다시 한번 스케일을 키워 돌아왔음을 환영했습니다. 특히 폐허가 된 도심의 고층 빌딩에서 벌어지는 좀비들의 집단 이동은 단순한 CG의 향연이 아니라, 마치 현대 무용을 보는 듯한 기괴하고도 아름다운 '안무(choreography)'라고 극찬했습니다."
            },
            {
                title: "🎭 감정이 메마른 화려한 시각적 롤러코스터",
                text: "그러나 문제는 스펙터클 그 자체에 있습니다. 좀비들의 움직임이 정교해지고 화려해질수록, 이를 피해 도망치는 인간 군상의 드라마는 얄팍해졌습니다. 인물들은 각자의 고뇌와 서사를 가질 여유 없이, 그저 다음 액션 시퀀스로 넘어가기 위한 체스말처럼 소비됩니다."
            },
            {
                quote: "연상호는 새롭고 끔찍한 쾌감을 선사하는 '좀비 안무'를 만들어냈다. 하지만 이 압도적인 스펙터클을 위해 영화의 감정적 코어는 희생되고 말았다.",
                quoteSource: "IndieWire, 데이비드 에를리치"
            }
        ]
    },
    colony_variety: {
        outlet: "📰 Variety — Full Review Translation",
        title: `"스스로의 무게에 비틀거리는 흥미로운 진화"`,
        author: "매기 리 (Variety 영화 평론가)",
        score: "★★★☆ (3.5)",
        gradient: "linear-gradient(135deg, #c0392b 0%, #8e44ad 100%)",
        link: "https://variety.com/",
        sections: [
            {
                title: "🎬 진화하는 K-좀비, 개념의 확장",
                text: "Variety의 매기 리는 연상호 감독의 <군체(Colony)>가 한국형 좀비(K-Zombie) 장르를 한 단계 더 흥미로운 방향으로 진화시켰다고 평가합니다. 단순히 육체적인 굶주림으로 움직이는 시체들이 아니라, '집단 지성'과 곤충의 '군집 행동'을 연상케 하는 좀비들의 유기적인 움직임은 그 자체로 시각적인 스펙터클이자 원초적인 공포를 불러일으킵니다."
            },
            {
                title: "🎭 거시적 서사와 미시적 감정의 불균형",
                text: "하지만 영화가 시도한 개념적 확장이 오히려 독이 된 측면도 존재합니다. 좁은 공간 안에서의 생존 투쟁과, 그들을 덮치는 거대한 '군체'의 개념을 설명하느라 정작 관객이 이입해야 할 인물들의 감정선은 희생되고 말았습니다. <부산행>에서 보여주었던 인물들 간의 끈끈한 유대감은 찾아보기 힘듭니다."
            },
            {
                quote: "K-좀비 장르의 매혹적인 진화. 집단 지성과 원초적 호러를 결합했지만, 가끔은 스스로가 만들어낸 거대한 개념적 무게에 짓눌려 비틀거리기도 한다.",
                quoteSource: "Variety, 매기 리"
            }
        ]
    }
};

// Modal Elements
const modalOverlay = document.getElementById('readerModal');
const modalHeader = document.getElementById('modalHeader');
const modalOutlet = document.getElementById('modalOutlet');
const modalTitle = document.getElementById('modalTitle');
const modalMetaAuthor = document.getElementById('modalMetaAuthor');
const modalMetaScore = document.getElementById('modalMetaScore');
const modalContent = document.getElementById('modalContent');
const modalSourceLink = document.getElementById('modalSourceLink');

// Open Modal and render specific critic review contents
function openReader(key) {
    const data = translatorData[key];
    if (!data) return;

    // Render Header with custom color/gradient
    modalHeader.style.background = data.gradient;
    modalOutlet.innerText = data.outlet;
    modalTitle.innerText = data.title;
    modalMetaAuthor.innerText = `작성자: ${data.author}`;
    modalMetaScore.innerText = `평점: ${data.score}`;
    
    // Render content sections dynamically
    modalContent.innerHTML = '';
    data.sections.forEach(sec => {
        if (sec.title && sec.text) {
            const sectionTitle = document.createElement('h3');
            sectionTitle.className = 'modal-section-title';
            sectionTitle.innerText = sec.title;
            
            const paragraph = document.createElement('p');
            paragraph.innerText = sec.text;

            modalContent.appendChild(sectionTitle);
            modalContent.appendChild(paragraph);
        } else if (sec.quote) {
            const quoteDiv = document.createElement('div');
            quoteDiv.className = 'modal-highlight-quote';
            
            const qParagraph = document.createElement('p');
            qParagraph.innerText = `"${sec.quote}"`;
            
            const qSource = document.createElement('div');
            qSource.className = 'modal-highlight-source';
            qSource.innerText = `— ${sec.quoteSource}`;

            quoteDiv.appendChild(qParagraph);
            quoteDiv.appendChild(qSource);
            modalContent.appendChild(quoteDiv);
        }
    });

    // Set link
    modalSourceLink.href = data.link;

    // Open overlay
    modalOverlay.classList.add('active');
    document.body.style.overflow = 'hidden'; // Lock main scroll
}

// Close Modal
function closeReader(event) {
    if (event && event.target !== modalOverlay && !event.target.classList.contains('modal-close-btn')) {
        return;
    }
    modalOverlay.classList.remove('active');
    document.body.style.overflow = 'auto'; // Unlock main scroll
}

// Animation helper to draw circular gauge bar
function animateGauge(circleId, score, maxScore) {
    const circle = document.getElementById(circleId);
    if (!circle) return;
    
    // Get total circumference from dasharray
    const strokeDasharray = parseFloat(window.getComputedStyle(circle).strokeDasharray);
    // Calculate offset based on score
    const progress = score / maxScore;
    const strokeDashoffset = strokeDasharray - (strokeDasharray * progress);
    
    // Set dashoffset dynamically with transition
    circle.style.strokeDashoffset = strokeDashoffset;
}

// Initialize and animate all gauges after window load
window.addEventListener('load', () => {
    // Delay slightly to allow the DOM rendering and CSS gradients
    setTimeout(() => {
        const isColony = (document.body.getAttribute('data-movie-key') === 'colony');
        const mainScore = isColony ? 7.2 : 8.5;
        const q1 = isColony ? 6.5 : 8.2;
        const q2 = isColony ? 7.5 : 8.6;
        const q3 = isColony ? 7.2 : 8.4;
        const q4 = isColony ? 7.6 : 8.8;

        // Main score (perimeter 440)
        animateGauge('mainGaugeFill', mainScore, 10.0);

        // Q1: 국내 평론가
        animateGauge('gaugeFillKrCritic', q1, 10.0);

        // Q2: 국내 관객
        animateGauge('gaugeFillKrAud', q2, 10.0);

        // Q3: 해외 평론가
        animateGauge('gaugeFillGlCritic', q3, 10.0);

        // Q4: 해외 관객
        animateGauge('gaugeFillGlAud', q4, 10.0);

        // TUG 극장별 추천 가이드 HUD 게이지 애니메이션 (최종 결재 승인 사양)
        const imaxFill = document.querySelector('.imax-gauge-fill');
        const dolbyFill = document.querySelector('.dolby-gauge-fill');
        const fourdxFill = document.querySelector('.fourdx-gauge-fill');
        const standardFill = document.querySelector('.standard-gauge-fill');
        
        if (imaxFill) imaxFill.style.width = isColony ? '85%' : '96%';
        if (dolbyFill) dolbyFill.style.width = isColony ? '92%' : '94%';
        if (fourdxFill) fourdxFill.style.width = isColony ? '80%' : '82%';
        if (standardFill) standardFill.style.width = isColony ? '78%' : '75%';
    }, 100);
});

// ESC Key close event for modal
window.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') {
        closeReader();
        closeClaraProfile();
        closeImagePreview();
    }
});

// Clara Harrison Profile Modal
const claraProfileModal = document.getElementById('claraProfileModal');
function openClaraProfile() {
    claraProfileModal.classList.add('active');
    document.body.style.overflow = 'hidden';
    setTimeout(() => {
        claraProfileModal.querySelectorAll('.cp-stat-fill').forEach(bar => {
            bar.style.width = bar.getAttribute('data-w');
        });
    }, 150);
}
function closeClaraProfile(event) {
    if (event && event.target !== claraProfileModal && !event.target.classList.contains('modal-close-btn')) return;
    claraProfileModal.classList.remove('active');
    document.body.style.overflow = 'auto';
}

// Image Preview Modal Functions
const imagePreviewModal = document.getElementById('imagePreviewModal');
const previewImage = document.getElementById('previewImage');
function openImagePreview() {
    imagePreviewModal.classList.add('active');
    document.body.style.overflow = 'hidden';
    setTimeout(() => {
        if (previewImage) previewImage.style.transform = 'scale(1)';
    }, 50);
}
function closeImagePreview(event) {
    if (event && event.target !== imagePreviewModal && !event.target.classList.contains('modal-close-btn')) return;
    if (previewImage) previewImage.style.transform = 'scale(0.95)';
    if (imagePreviewModal) imagePreviewModal.classList.remove('active');
    // Restore page overflow scroll if Clara profile is closed too, otherwise keep hidden
    if (!claraProfileModal.classList.contains('active')) {
        document.body.style.overflow = 'auto';
    }
}

// My Rating Interactive Logic
document.addEventListener('DOMContentLoaded', () => {
    // Sort theater metric rows in descending order of score dynamically
    const theaterGuideCard = document.getElementById('theaterGuideCard');
    if (theaterGuideCard) {
        const rows = Array.from(theaterGuideCard.querySelectorAll('.theater-metric-row'));
        const parseScore = (row) => {
            const scoreSpan = row.querySelector('.theater-metric-score');
            if (!scoreSpan) return 0;
            const text = scoreSpan.textContent;
            const match = text.match(/([0-9.]+)\s*\/\s*10/);
            return match ? parseFloat(match[1]) : parseFloat(text) || 0;
        };
        const hasValidScores = rows.some(r => {
            const val = parseScore(r);
            return !isNaN(val) && val > 0;
        });
        if (hasValidScores) {
            rows.sort((a, b) => parseScore(b) - parseScore(a));
            rows.forEach(row => theaterGuideCard.appendChild(row));
        }
    }

    const stars = document.querySelectorAll('#interactiveStars span');
    const scoreDisplay = document.getElementById('myRatingScore');
    const saveBtn = document.getElementById('saveRatingBtn');
    const confirmMsg = document.getElementById('saveConfirmMsg');

    const movieKey = document.body.getAttribute('data-movie-key') || 'mandalorian';
    const MOVIE_KEY = 'tug_rating_' + movieKey;
    let currentScore = 0;

    // Update star visuals
    const updateStars = (score) => {
        stars.forEach(s => {
            const val = parseInt(s.getAttribute('data-val'));
            s.classList.remove('active', 'half');
            s.style.background = '';
            s.style.webkitBackgroundClip = '';
            s.style.webkitTextFillColor = '';
            
            if (val <= score) {
                s.classList.add('active');
            }
        });
    };

    const commentInput = document.getElementById('commentInput');

    // Load from localStorage
    const loadRating = () => {
        const saved = localStorage.getItem(MOVIE_KEY);
        if (saved) {
            const data = JSON.parse(saved);
            currentScore = data.score || 0;
            scoreDisplay.textContent = currentScore.toFixed(1);
            updateStars(currentScore);
            if (commentInput) commentInput.value = data.comment || '';
        } else {
            currentScore = 0;
            scoreDisplay.textContent = "0.0";
            updateStars(0);
            if (commentInput) commentInput.value = '';
        }
    };

    // Save rating to localStorage & cloud helper
    const saveRatingLocallyAndCloud = () => {
        const commentText = commentInput ? commentInput.value.trim() : '';
        if (currentScore === 0 && commentText === '') {
            localStorage.removeItem(MOVIE_KEY);
            pushToCloud(CLOUD_KEY, null);
            pushValueToCloud(CLOUD_KEY.replace('rating', 'comment'), '0');
        } else {
            const data = { score: currentScore, comment: commentText };
            localStorage.setItem(MOVIE_KEY, JSON.stringify(data));
            pushToCloud(CLOUD_KEY, data);
            pushValueToCloud(CLOUD_KEY.replace('rating', 'comment'), safeBtoa(commentText));
        }
    };

    // Star click (whole stars only)
    stars.forEach(star => {
        star.addEventListener('click', (e) => {
            const val = parseInt(star.getAttribute('data-val'));
            // val === 2 means first star; clicking it again resets to 0
            if (val === 2 && currentScore === 2) {
                currentScore = 0;
            } else {
                currentScore = val;
            }
            scoreDisplay.textContent = currentScore.toFixed(1);
            updateStars(currentScore);
            saveRatingLocallyAndCloud();
        });

        // Hover preview (whole stars only)
        star.addEventListener('mousemove', (e) => {
            const val = parseInt(star.getAttribute('data-val'));
            stars.forEach(s => {
                const sVal = parseInt(s.getAttribute('data-val'));
                s.classList.remove('active', 'half');
                s.style.background = '';
                s.style.webkitBackgroundClip = '';
                s.style.webkitTextFillColor = '';
                if (sVal <= val) s.classList.add('active');
            });
        });

        star.addEventListener('mouseleave', () => {
            updateStars(currentScore);
        });
    });

    const SYNC_TOKEN = "9cmvofbs";
    const SYNC_BASE = "https://keyvalue.immanuel.co/api/KeyVal";
    const CLOUD_KEY = MOVIE_KEY.replace('tug_rating_', '') + '_rating';

    function safeBtoa(str) {
        if (!str) return "";
        try {
            const utf8Str = unescape(encodeURIComponent(str));
            const base64 = btoa(utf8Str);
            return base64.replace(/\+/g, '-').replace(/\//g, '_').replace(/=/g, '');
        } catch(e) {
            return "";
        }
    }

    function safeAtob(safeBase64) {
        if (!safeBase64) return "";
        let base64 = safeBase64.replace(/-/g, '+').replace(/_/g, '/');
        while (base64.length % 4) {
            base64 += '=';
        }
        try {
            const utf8Str = atob(base64);
            return decodeURIComponent(escape(utf8Str));
        } catch(e) {
            return "";
        }
    }

    function pushValueToCloud(key, val) {
        fetch(`${SYNC_BASE}/UpdateValue/${SYNC_TOKEN}/${key}/${val}`, {
            method: 'POST',
            body: '0'
        }).catch(e => console.error('TUG Cloud Sync: push failed for ' + key, e));
    }

    function pushToCloud(key, value) {
        let cloudVal = "0";
        if (value && typeof value === 'object' && value.score !== undefined) {
            cloudVal = String(value.score).replace('.', '_');
        } else if (value && typeof value === 'string') {
            try {
                const parsed = JSON.parse(value);
                cloudVal = String(parsed.score || 0).replace('.', '_');
            } catch(e) {
                cloudVal = value.replace('.', '_');
            }
        } else {
            cloudVal = "0";
        }
        pushValueToCloud(key, cloudVal);
    }

    const syncWithCloud = () => {
        fetch(`${SYNC_BASE}/GetValue/${SYNC_TOKEN}/${CLOUD_KEY}`)
            .then(res => res.text())
            .then(text => {
                if (text && text.trim() !== "" && text !== "null") {
                    let cleanVal = text.trim().replace(/^"|"$/g, '');
                    if (cleanVal === "0" || cleanVal === "") {
                        if (localStorage.getItem(MOVIE_KEY)) {
                            localStorage.removeItem(MOVIE_KEY);
                            currentScore = 0;
                            scoreDisplay.textContent = "0.0";
                            updateStars(0);
                            if (commentInput) commentInput.value = "";
                        }
                        return;
                    }
                    const score = parseFloat(cleanVal.replace('_', '.'));
                    
                    // Fetch comment
                    const commentKey = CLOUD_KEY.replace('rating', 'comment');
                    fetch(`${SYNC_BASE}/GetValue/${SYNC_TOKEN}/${commentKey}`)
                        .then(res2 => res2.text())
                        .then(text2 => {
                            let commentText = "";
                            if (text2 && text2.trim() !== "" && text2 !== "null") {
                                let cleanComment = text2.trim().replace(/^"|"$/g, '');
                                commentText = safeAtob(cleanComment);
                            }

                            const localData = JSON.stringify({ score: score, comment: commentText });
                            const saved = localStorage.getItem(MOVIE_KEY);
                            let needUpdate = false;
                            if (!saved) {
                                needUpdate = true;
                            } else {
                                try {
                                    const parsed = JSON.parse(saved);
                                    if (parsed.score !== score || parsed.comment !== commentText) {
                                        needUpdate = true;
                                    }
                                } catch(e) {
                                    needUpdate = true;
                                }
                            }

                            if (needUpdate) {
                                localStorage.setItem(MOVIE_KEY, localData);
                                currentScore = score;
                                scoreDisplay.textContent = currentScore.toFixed(1);
                                updateStars(currentScore);
                                if (commentInput) commentInput.value = commentText;
                            }
                        });
                }
            })
            .catch(e => console.warn('TUG Cloud Sync: pull failed'));
    };

    // Save button
    saveBtn.addEventListener('click', () => {
        saveRatingLocallyAndCloud();
        confirmMsg.classList.add('show');
        setTimeout(() => {
            confirmMsg.classList.remove('show');
        }, 2000);
    });

    loadRating();
    syncWithCloud();
    setInterval(syncWithCloud, 3000);

    // Sync instantly when another tab (e.g. mobile dashboard) updates localStorage
    window.addEventListener('storage', (e) => {
        if (e.key === MOVIE_KEY) loadRating();
    });
});
